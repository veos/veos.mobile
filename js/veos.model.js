/*jshint browser: true, devel: true */

window.veos = (function(veos) {
    var self = {};

    /* put your functions and stuff here */

    // for example:
    self.someFunction = function (foo) {
        console.log("stuff");
    };

    veos.model = self;
    return veos;
})(window.veos || {});